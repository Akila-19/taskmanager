# taskmanager-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: taskmanager-service
  namespace: taskmanager
spec:
  selector:
    app: taskmanager
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
